---

- name: Update Known Hosts
  shell: ../scripts/known.hosts.sh "{{ args_proxmox_host }}" "{{ args_local_user_ssh }}"
  args:
    executable: /bin/bash
  register: expectresult
  changed_when: '"Changed" in expectresult.stdout'
  failed_when: '"Error" in expectresult.stdout'
  connection: local
