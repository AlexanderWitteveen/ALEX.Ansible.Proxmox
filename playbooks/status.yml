---

- name: "Playbook: Get Status from VM/Template"
  hosts: "{{ args_host }}"
  gather_facts: false

  vars_files:
    - /etc/ansible/inventory/vars.yml

  roles:
    - name: alex.proxmox.status
      vars:
        args_vmid: "{{ args_proxmox_vmid }}"
      failed_when: '"NOTEXIST" not in vmstatusresult.stdout'

